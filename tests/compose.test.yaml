services:
  app:
    image: corpo-knowledge-app:latest
    environment:
      ADMIN_USERNAME: testadmin
      ADMIN_PASSWORD: integration-test-password-123
      APP_ORIGIN: http://localhost:3001
    ports: !override
      - "127.0.0.1:3001:3000"
